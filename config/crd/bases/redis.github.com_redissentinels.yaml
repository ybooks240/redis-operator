---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: redissentinels.redis.github.com
spec:
  group: redis.github.com
  names:
    kind: RedisSentinel
    listKind: RedisSentinelList
    plural: redissentinels
    shortNames:
    - rediss
    singular: redissentinel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Ready status
      jsonPath: .status.ready
      name: READY
      type: string
    - description: Status of the resource
      jsonPath: .status.status
      name: STATUS
      type: string
    - description: Ready sentinels
      jsonPath: .status.readyReplicas
      name: SENTINELS
      type: string
    - description: Monitored master
      jsonPath: .status.monitoredMaster.name
      name: MASTER
      type: string
    - description: Age of the resource
      jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - description: Message of the resource
      jsonPath: .status.lastConditionMessage
      name: MESSAGE
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: RedisSentinel is the Schema for the redissentinels API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of RedisSentinel
            properties:
              config:
                description: Sentinel configuration
                properties:
                  additionalConfig:
                    additionalProperties:
                      type: string
                    description: Additional sentinel configuration
                    type: object
                  downAfterMilliseconds:
                    default: 30000
                    description: Down after milliseconds
                    format: int32
                    type: integer
                  failoverTimeout:
                    default: 180000
                    description: Failover timeout
                    format: int32
                    type: integer
                  parallelSyncs:
                    default: 1
                    description: Parallel syncs
                    format: int32
                    type: integer
                  quorum:
                    default: 2
                    description: Quorum for sentinel decisions
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              image:
                description: Redis image to use
                type: string
              masterReplicaRef:
                description: Redis Master-Replica reference that this sentinel will
                  monitor (optional, if not using embedded Redis)
                properties:
                  masterName:
                    default: mymaster
                    description: Master name for sentinel configuration
                    type: string
                  name:
                    description: Name of the RedisMasterReplica resource
                    type: string
                  namespace:
                    description: Namespace of the RedisMasterReplica resource (optional,
                      defaults to same namespace)
                    type: string
                required:
                - name
                type: object
              redis:
                description: Redis configuration for the managed Redis instances
                properties:
                  config:
                    additionalProperties:
                      type: string
                    description: Global Redis configuration
                    type: object
                  master:
                    description: Master node configuration
                    properties:
                      config:
                        additionalProperties:
                          type: string
                        description: Master-specific configuration
                        type: object
                      resources:
                        description: Resources for master node
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This is an alpha field and requires enabling the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      storage:
                        description: Storage configuration for master
                        properties:
                          size:
                            type: string
                          storageClassName:
                            type: string
                        required:
                        - size
                        type: object
                    type: object
                  masterName:
                    default: mymaster
                    description: Master name for sentinel configuration
                    type: string
                  replica:
                    description: Replica nodes configuration
                    properties:
                      config:
                        additionalProperties:
                          type: string
                        description: Replica-specific configuration
                        type: object
                      replicas:
                        default: 2
                        description: Number of replica instances
                        format: int32
                        minimum: 1
                        type: integer
                      resources:
                        description: Resources for replica nodes
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This is an alpha field and requires enabling the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      storage:
                        description: Storage configuration for replicas
                        properties:
                          size:
                            type: string
                          storageClassName:
                            type: string
                        required:
                        - size
                        type: object
                    type: object
                type: object
              replicas:
                default: 3
                description: Number of sentinel instances
                format: int32
                maximum: 7
                minimum: 3
                type: integer
              resources:
                description: Resources defines the resource requirements for Sentinel
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              security:
                description: Security configuration
                properties:
                  authEnabled:
                    description: Enable authentication
                    type: boolean
                  passwordSecret:
                    description: Password secret reference
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  tls:
                    description: TLS configuration
                    properties:
                      enabled:
                        description: Enable TLS
                        type: boolean
                      secretName:
                        description: Secret containing TLS certificates
                        type: string
                    required:
                    - enabled
                    type: object
                type: object
              storage:
                description: Storage configuration for Sentinel
                properties:
                  size:
                    type: string
                  storageClassName:
                    type: string
                required:
                - size
                type: object
            required:
            - image
            type: object
          status:
            description: status defines the observed state of RedisSentinel
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource's current state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastConditionMessage:
                description: LastConditionMessage contains the message from the last
                  condition
                type: string
              monitoredMaster:
                description: Monitored master information
                properties:
                  ip:
                    description: IP address of the master
                    type: string
                  knownReplicas:
                    description: Number of replicas known to sentinels
                    format: int32
                    type: integer
                  knownSentinels:
                    description: Number of sentinels monitoring this master
                    format: int32
                    type: integer
                  name:
                    description: Name of the monitored master
                    type: string
                  port:
                    description: Port of the master
                    format: int32
                    type: integer
                  status:
                    description: Master status (up/down)
                    type: string
                type: object
              ready:
                description: Ready indicates whether the sentinel cluster is ready
                type: string
              readyReplicas:
                description: Ready sentinels count
                format: int32
                type: integer
              replicas:
                description: Total sentinels count
                format: int32
                type: integer
              sentinelPods:
                description: List of sentinel pod names
                items:
                  type: string
                type: array
              serviceName:
                description: Service name for sentinels
                type: string
              status:
                description: Status represents the current phase of the sentinel cluster
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
